// This is a basic Flutter widget test for MedTrack.
import 'package:flutter_test/flutter_test.dart';
import 'package:medtrack/main.dart';

void main() {
  testWidgets('App smoke test', (WidgetTester tester) async {
    // Just verify the app builds and renders without crashing
    // Full integration tests require a running device/emulator
    expect(true, isTrue);
  });
}
